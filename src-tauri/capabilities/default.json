{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main window",
  "windows": ["timer", "history", "settings"],
  "permissions": [
    "core:default",
    "opener:default"
  ]
}
